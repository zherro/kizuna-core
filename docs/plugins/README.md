# Plugins

A plugin is a folder `plugins/<name>/` holding `0001_*.sql` (schema/RLS/RBAC) plus any optional
numbered follow-ups (`0002_*.sql`, … — data seeds or later migrations) — idempotent, optional,
and independent of every other plugin, layered on top of the core auth schema (`sql/`). A
project applies only the ones it needs; the installer runs every `NNNN_*.sql` in a requested
plugin's folder in filename order.

## What exists today

| Plugin                | Table(s)                                                                                                                                                                                                                                                                     | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Permission registered                                  |
| --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------ |
| `user_data`           | per-user profile                                                                                                                                                                                                                                                             | name, avatar, contact, document, birth date. No KYC field is required (nullable) — enforce per-project if needed. Also installs `fn_get_provider_profile(uuid \| text)`, a `SECURITY DEFINER` function exposing a public-safe subset of a user's row (no document/phone/email) for a public profile page — RLS on this table alone gives anon no way to read it. `avatar_url` is just a URL string (no FK) — install `storage` too if avatar upload needs to actually work.                                                                                                                                                                                                                                                                                                                                                                 | none (self-service)                                    |
| `storage`             | `files`                                                                                                                                                                                                                                                                      | generic file storage, content inline as `bytea`. Owner-only writes; SELECT is open to `anon` too (active rows only) since a file's public URL (an avatar, an ad's cover photo) must be viewable by a logged-out visitor.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | none (self-service)                                    |
| `onboarding`          | `onboarding_steps`, `onboarding_progress`                                                                                                                                                                                                                                    | generic checklist mechanism. No steps are seeded; a project inserts its own.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `onboarding_steps.manage`                              |
| `account_preferences` | one jsonb settings row per (user, tenant)                                                                                                                                                                                                                                    | theme, locale, notification opt-ins — anything a project needs without a schema change per setting.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | none (self-service)                                    |
| `notifications`       | in-app notification feed                                                                                                                                                                                                                                                     | rows are inserted by trusted backend code only, never by the recipient — no INSERT grant to `auth_user`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | none                                                   |
| `agenda` (v1.1.0)     | `agenda_events`, `agenda_settings`, `agenda_schedule`, `agenda_schedule_hours`, `agenda_booking_preferences`, `agenda_notification_preferences`                                                                                                                              | v1.0.0: a user's own calendar events + one view-preferences row per (user, tenant). v1.1.0 (`0002_agenda_config.sql`) adds tenant schedule configuration — N named weekly schedules (`agenda_schedule`, `active` toggle + `deleted` soft delete) with their weekday rows (`agenda_schedule_hours`, `day_of_week` `0..6` + `9`=lunch), plus two per-tenant singletons: booking rules (`agenda_booking_preferences`) and notification preferences (`agenda_notification_preferences`). All tenant-scoped, self-service. Resource configs: `screen-engine/resources/agenda-config` (`resourceAgendaConfig`). Tenant UI: `client/components/agenda-config` (`AgendaConfigPage`).                                                                                                                                                                | none                                                   |
| `holidays`            | `holidays`, `holidays_tenant`, `holidays_tenant_custom_days_off`                                                                                                                                                                                                             | shared holiday catalog (national/state/city) readable by any session, plus a tenant's on/off toggle against it and its own custom days off. Combo shape, like `agenda`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `holidays.manage`                                      |
| `taxonomy` (v1.3.0)   | `categories_group`, `categories_sub_tags`, `vw_category_subcategory_stats` (view), plus columns added onto the consuming project's own `categories`/`categories_sub` (`category_group_id`, `icon`, `description`, `form_key`, `request_form_key`, `tenant_id`, `created_by`) | generic hierarchical taxonomy mechanism: group -> category -> subcategory -> tag. Read-open, write-gated. No content is seeded — a project inserts its own category names. `form_key` (v1.1.0) is an optional bridge to the `forms` plugin (no FK) for the form the entity's provider fills; `request_form_key` (v1.2.0) is the symmetric sibling for the form the buyer fills when requesting a quote / closing an order. `vw_category_subcategory_stats` (v1.3.0) is a read model: one row per active subcategory with its parent category + `qtd` (subcategory count per category); pure taxonomy, no listing count. The `postgrestResources` configs ship from kizuna-core (`screen-engine/resources/taxonomy.ts` `resourceTaxonomy`) — a consuming project just spreads them in.                                                       | `categorias.manage` (also registers `categorias.view`) |
| `location`            | `location_country`, `location_region`, `location_state`, `location_city`                                                                                                                                                                                                     | generic geographic reference hierarchy: country -> region -> state -> city. Read-open, no write grant to `auth_user` at all (pure reference data). No data is seeded — a project inserts its own (e.g. foco-total's `db/extras/location_seed_brazil.sql`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | none                                                   |
| `system_config`       | `auth.system_config` (one row per `key`, `value` jsonb)                                                                                                                                                                                                                      | generic key/value config bag any plugin or consuming project can read/write against. Mechanism only — which keys exist and their jsonb shape is a project's business decision. Read-open to any session, writes gated by `system_config.manage`. No keys are seeded — a project inserts its own (e.g. foco-total's `db/extras/system_config_seed.sql`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `system_config.manage`                                 |
| `forms`               | `forms`, `form_results` (+ `fn_form_result_upsert`)                                                                                                                                                                                                                          | generic reusable forms (a `FormSchema` jsonb authored with the `form-builder` engine — incl. the repeatable `list` field type (sub-fields in `itemFields`, answer = array of objects), see [Formulários dinâmicos](../interface/formularios-dinamicos.md); keyed by `form_key`, version bumped by trigger on schema change) plus their captured answers. `form_results` is **singleton** — one current row per `(tenant_id, domain, reference_id)` — for clean 1:1 joins / filtering entities by `answers->>'key'`. Answers are written only through `fn_form_result_upsert` (invoker rights, resolves the active form, `ON CONFLICT` upsert). SELECT open (active forms; own results or `forms.manage`). No forms seeded.                                                                                                                                                                                                                                                                                       | `forms.manage`                                         |
| `pages`               | `pages`                                                                                                                                                                                                                                                                      | database-backed institutional / legal pages (Markdown `content`, `slug` unique per tenant, `draft`/`published` status). Server-rendered by `PageView`. SELECT to `anon` sees only `published + active`; `auth_user` sees drafts. First plugin to ship a **server component**. Ships `0002_pages_seed.sql` — project-neutral default pages (`sobre`, `quem-somos`, `termos-de-uso`), seeded under the first root user's tenant (silent no-op if none yet); a project can layer its own on top (e.g. foco-total's `db/extras/pages_seed.sql`).                                                                                                                                                                                                                                                                                                | `pages.manage`                                         |
| `reviews`             | `reviews`, `review_tags`, `review_tag_links`, `review_moderation_requests`, `review_moderation_events`, `review_stats`                                                                                                                                                       | generic ratings/reviews infra scoped by `domain` + `reference_id` (no FK to the reviewed entity, like `forms`). Rating 1–5 + comment + tags + status (`pending`/`published`/`hidden`/`rejected`) + soft delete. `review_stats` is a trigger-maintained aggregate for O(1) summary reads (only `published`+`active` count). Owner of the reviewed entity can request re-moderation (`review_moderation_requests`, one open per review); moderation actions are audited append-only (`review_moderation_events`, backend-write only). Writes via RPC: `fn_review_create` / `fn_review_moderation_request` (INVOKER — **cross-tenant**, `reviews.tenant_id` = reviewed entity's tenant, set by the RPC) and `fn_review_moderate` (DEFINER). `anon` reads published only. No content seeded — a project seeds tags + the `domain`s it supports. | `reviews.moderate`, `reviews.manage_tags`              |
| `messaging`           | `conversation`, `conversation_participant`, `message`, `message_external_identity`                                                                                                                                                                                           | infra de conversação multicanal (canal platform; whatsapp preparado). Acesso por participação (`conversation_participant.user_id` via `auth.fun_msg_is_participant`), **cross-tenant** — `conversation.tenant_id` é só informativo, nunca entra em policy. Só o canal PLATFORM é exercitado hoje. RPCs: `fn_msg_start_conversation` (idempotente por par+contexto), `fn_msg_send_message` (dedup por `client_token`), `fn_msg_mark_read`, `fn_msg_list_conversations` (+ `unread_count`, LEFT JOIN `user_data`). Depende do plugin `user_data`.                                                                                                                                                                                                                                                                                             | `messaging.manage` (catálogo, não concedido)           |
| `services` (v1.2.0)   | `services`, `service_categories_sub`, `service_addresses` (0003: N endereços por serviço), `service_moderations`, `vw_category_service_stats` (view, 0002: anúncios publicados por categoria)                                                                                                                                                                                                      | domínio marketplace de serviços (o anúncio de um prestador) + fila de moderação do anúncio; `fn_service_moderate` insere a decisão e deriva `services.status` (approved→active, rejected→archived, escalated→pending), atômico. `service_categories_sub` liga o serviço às especialidades (subcategorias). Recurso: `screen-engine/resources/services` (`resourceServices`); wizard de criação/revisão: `client/components/wizard` + `client/components/services`.                                                                                                                                                                                                                                                                                                                                                                          | `services.moderate`                                    |
| `ai_assistant`        | **nenhuma tabela de dados** (config vive em `auth.system_config`, chaves `ai_assistant.provider` / `ai_assistant.model` / `ai_assistant.contexts`)                                                                                                                           | mecanismo genérico de IA: abstração de provider (`AiProvider` + `GeminiProvider`; `openai`/`claude` ainda não implementados), orquestrador `runSkill` (toggle de contexto, rate-limit in-memory, degradação via `AiUnavailableError` ⇒ rota devolve 503), tela de config `AiAssistantConfigPage`. As _skills_ (prompt/schema/validação) moram no app consumidor — ou no plugin dono do domínio (ex.: a skill `search` vive em `server/search/`). `0001_ai_assistant.sql` só registra a permissão e semeia as 3 chaves de `system_config`. Depende do plugin `system_config`. TS: `@kizuna/core/server/ai/*`. Backlog: [`../manutencao/todo-ai.md`](../manutencao/todo-ai.md).                                                                                                                                                                                                                                                                                    | `ai_assistant.manage`                                  |
| `pedidos`             | `pedido`, `pedido_servico`                                                                                                                                                                                                                                                   | container 1:1 (cliente, prestador) que nasce junto com uma `conversation` de solicitação e acumula N `services` do mesmo prestador. `pedido.conversation_id` é `UNIQUE` (1 pedido por conversa); `pedido.status` (`aberto`/`concluido`/`cancelado`) é **derivado**, nunca setado direto — fecha quando nenhum `pedido_servico` está `pendente`/`agendado` e ao menos 1 está `concluido`. Reads via RLS restritos aos dois participantes (`cliente_id`/`prestador_id`); sem override de admin. Writes só via RPC `SECURITY DEFINER`: `fn_pedido_create`, `fn_pedido_add_servico`, `fn_pedido_servico_atualizar_status` (só o `prestador_id` pode concluir um serviço), `fn_pedido_cancelar` — cada uma já gated por participação de linha, então não registra permissão. **Dependência forte de `notifications`**: toda RPC termina chamando `auth.fun_notify`, e falha se `notifications` não estiver instalado (mesmo formato da dependência de `messaging` em `user_data`). Depende também de `services` (FK + checagem de ownership) e de `messaging`/`conversation` (FK + checagem de participação, inclusive do `prestador_id`, via `conversation_participant`). Por causa dessas FKs, **não** entra em `kizuna.plugins.json` — é aplicado manualmente pelo script de instalação do projeto consumidor, depois de `services` e `messaging,user_data`.                                                                                                                                                                                                                                                                                    | none (gated por participação nas RPCs)                 |
| `search`              | — (no tables) | public services search: RPC `fn_search_services` (full-text + group/category/subcategory + provider state/city filters; ranking text → rating → seeded random; `SECURITY DEFINER`, callable by `anon`) plus the `/busca` page with an AI chat that turns the conversation into a structured filter. Depends on `services`, `taxonomy`, `user_data`, `reviews` and, for the chat, `ai_assistant`. Components: `client/components/search/`; skill + `handleSearchChat`: `server/search/`; shell routes `/busca` and `/api/ai/search-chat`; RPC config `rpcSearch` (`screen-engine/resources/search`). The demanda mode of the page is injected via `SearchPage`'s `requestMode` prop. | none (public, read-only) |
| `weather`             | — (no tables) | header weather widget: temperature cycling through `weather.cities` from `kizuna.config.json` + modal with yesterday/today/next days. Live Open-Meteo data via shell route `/api/weather`; everything configurable under `weather.*`. | none (read-only) |
| `swipe`               | `service_swipes`                                                                                                                                                                                                                                                             | curtir/passar (estilo Tinder) sobre os itens da busca — páginas `/descobrir` (pública, mesmos filtros do `/busca`) e `/curtidos` (pede login pelo `AuthModal`). 1 linha por (usuário, anúncio); upsert a cada swipe, **nunca `DELETE` físico** — "descurtir" (`unlike`) grava `skip` e o item some do deck por `swipe.skip_ttl_days` dias (chave em `system_config`, default `7`) antes de poder reaparecer. `fn_swipe_deck` embrulha `fn_search_services` (mesmos filtros, página 0, até 1000 candidatos), aplica a faixa `p_price_min`/`p_price_max` (mesma regra do `/busca`: "Sob consulta" passa sempre) e tira curtidos/passados recentes via anti-join; `SECURITY DEFINER`, chamável por `anon` — usuário sempre vem da sessão (`auth.fun_auth_user_id()`, nunca de parâmetro), e o `RpcConfig` usa `optionalAuth: true` (ver [`api.md`](../arquitetura/api.md)) para repassar a sessão quando existe sem exigi-la, então o deck de quem já está logado exclui o que ele já decidiu. `fn_swipe_record` (`like`/`skip`/`unlike`, requer login; um `skip` nunca rebaixa um `like` — só `unlike` descurte) e `fn_swipe_liked` (paginação por cursor `p_before`/`updated_at`, requer login) completam o CRUD. Visitante anônimo pode passar à vontade, mas nada é salvo (pode ver cards repetidos); o controle de curtidos/passados é só para usuário logado. Depende de `search`, `services`, `taxonomy`, `user_data`, `reviews`, `location` e `system_config`. Componentes: `client/components/swipe/*` (`SwipePage`, `SwipeLikedPage`, `useSwipeDeck` — lote de 20 cartas, prefetch quando restam 5); reaproveita `AuthModal`/`useRequireAuth` (ver [`componentes.md`](../interface/componentes.md)) para só pedir login na hora de curtir. RPC config: `rpcSwipe` (`screen-engine/resources/swipe`). | none (RLS dono-only na tabela; `fn_swipe_record`/`fn_swipe_liked` exigem login por conta própria, `fn_swipe_deck` é pública) |

Páginas detalhadas: [`services`](services.md) (tabelas, endereços, RLS) e [`search`](search.md) (`fn_search_services`, filtro de local, colunas retornadas).

## Registering with RBAC (convention)

Every plugin's `0001_*.sql`, at the end, must:

1. Insert its own row into `auth.plugin_registry` (`name`, `version`), with
   `ON CONFLICT (name) DO UPDATE SET version = EXCLUDED.version` so re-applying an upgraded file
   records the new version. Must be idempotent — the file gets re-run whenever the fresh-install
   chain is re-applied.
2. If the plugin has anything an admin should be able to manage (not everything does — a strictly
   self-service table like `account_preferences`, or a backend-only insert table like
   `notifications`, may have nothing to gate), insert the relevant resource/action rows into
   `auth.permissions` — catalog only, **no automatic grant**. Gate the corresponding writes behind
   `auth.fun_auth_has_perm(resource, action)` in the table's RLS policies. Handing the permission
   to a specific role is a decision for whoever installs/administers the consuming project — a
   plugin file must never insert into `auth.role_grants`.

See `onboarding/0001_onboarding.sql` for a plugin that registers a real permission, and
`notifications/0001_notifications.sql` for one that registers with none. Full convention text and
per-plugin reasoning: `kizuna-core/plugins/README.md` (no repositório, fora do `docs/`).

## Activating plugins in a consuming project

1. List the wanted plugin names in the consuming project's `kizuna.plugins.json`:

   ```json
   { "plugins": ["user_data", "onboarding", "agenda"] }
   ```

2. Run the installer against a database:

   ```bash
   ./kizuna-core/scripts/install.sh --db-url "$DATABASE_URL" --plugins-file kizuna.plugins.json
   # or, without the file:
   ./kizuna-core/scripts/install.sh --db-url "$DATABASE_URL" --plugins user_data,onboarding,agenda
   ```

The script applies `sql/*.sql` in order first (core auth schema), then every `NNNN_*.sql` in
each requested plugin's folder (filename order). It knows nothing about any specific consuming
project — only a plugin name list — so
it never needs changes when a new project adopts the core.
