# Kizuna Core

SQL migrations for the auth/multi-tenancy/RBAC core shared across projects built on this
stack (PostgreSQL + PostgREST + RLS). See `sql/` for migrations, applied in filename order.
Design record: see the consuming project's `docs/superpowers/specs/2026-08-30-kizuna-auth-model-design.md`.
