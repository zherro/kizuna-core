/** Resumo do que já foi preenchido nos passos anteriores. Dois usos:
 *  - `compact`: cartão fino — fixo no trilho lateral (desktop) e acima do conteúdo (mobile).
 *  - completo (default): mini-preview no passo de Descrição, com as fotos em quadradinhos. */
export declare function ServiceConfigSummary({ groupName, categoryName, subcategoryNames, title, serviceLocation, startingPrice, priceUnit, imageIds, compact, }: {
    groupName?: string;
    categoryName?: string;
    subcategoryNames: string[];
    title: string;
    serviceLocation: string;
    startingPrice: number;
    priceUnit: string;
    imageIds?: string[];
    compact?: boolean;
}): import("react/jsx-runtime").JSX.Element;
//# sourceMappingURL=service-config-summary.d.ts.map